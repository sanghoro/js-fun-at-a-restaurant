// 1. adding new orders to an order type list
// 1a. needs two inputs; order # and object array of orders.
// 1b. needs to add whole object to array as orders will come in as objects
// 1c. input = object; object


function takeOrder(orderNumber, deliveryOrders) {
  console.log()

}


module.exports = {
  takeOrder,
  // refundOrder,
  // listItems,
  // searchOrder
}